# ── Web framework ─────────────────────────────────────────────────────────────
fastapi==0.115.6          # core framework
uvicorn[standard]==0.32.1 # ASGI server (includes websockets + watchfiles for --reload)

# ── Data validation ────────────────────────────────────────────────────────────
pydantic==2.10.3          # response/request schema validation (bundled with FastAPI)

# ── Optional but recommended for production ───────────────────────────────────
python-dotenv==1.0.1      # load .env files (API keys, config)
google-generativeai
python-multipart==0.0.9